<navigation:Page 
  x:Class="IL.View.About" 
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" 
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" 
  xmlns:navigation="clr-namespace:System.Windows.Controls;assembly=System.Windows.Controls.Navigation"
  Style="{StaticResource PageStyle}">

  <Grid x:Name="LayoutRoot">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="*"/>
    </Grid.RowDefinitions>
    <!--<ScrollViewer x:Name="PageScrollViewer" Style="{StaticResource PageScrollViewerStyle}">

      <StackPanel x:Name="ContentStackPanel">

        <TextBlock x:Name="HeaderText" Style="{StaticResource HeaderTextStyle}" Text="About" />
        <TextBlock x:Name="ContentText" Style="{StaticResource ContentTextStyle}" Text="About page content" />

      </StackPanel>

    </ScrollViewer>-->
    <TextBlock x:Name="HeaderText" Grid.Row="0" Style="{StaticResource HeaderTextStyle}" Text="About" />
    <TextBlock x:Name="VersionNumber" Grid.Row="1" Margin="0,5"/>
    <Border Grid.Row="2">
      <TextBlock Text="IL.View is an open-source Silverlight .NET assembly browser and decompiler."/>
    </Border>
  </Grid>

</navigation:Page>